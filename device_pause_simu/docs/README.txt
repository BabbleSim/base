This device just connects to a phy and pauses the simulation in some given
instants

Used for testing of the phy, or for debugging (note that you can let a
simulation run until a given moment, pause, plug a debugger to one of the
devices, and then continue)
